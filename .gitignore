# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
SETUP.md
design

# the example env is a template, not a secret — keep it committed
!.env.local.example

# service account credentials — never commit
*firebase-adminsdk*.json
*service-account*.json
serviceAccountKey*.json

# firebase cli
.firebase/
firebase-debug.log*
firestore-debug.log*
database-debug.log*
pubsub-debug.log*
ui-debug.log*

# os
Thumbs.db
desktop.ini

# claude code local overrides
.claude/settings.local.json

# point-in-time firestore dumps
firestore-backup-*.json
